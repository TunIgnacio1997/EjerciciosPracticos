📘 README — Cómo correr el proyecto

Este es un proyecto creado con React utilizando Vite. Para ejecutarlo en tu máquina local, sigue los pasos a continuación.

🚀 Cómo correr el proyecto

Asegúrate de tener instalado Node.js (versión 16 o superior).

Clona el repositorio y entra al directorio del proyecto.

Instala las dependencias con npm install.

Inicia el servidor de desarrollo usando npm run dev.

Abre tu navegador y entra a la URL que aparece en la consola, normalmente http://localhost:5173.

Para generar una versión optimizada para producción, ejecuta npm run build. Esto creará la carpeta dist con los archivos listos para desplegar en un hosting.